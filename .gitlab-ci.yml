variables:
   BRANCH_NAME: "master"

stages:
  - sca
  - build

veracode_agent_sca:
  stage: sca
  script: 
    curl -sSL https://download.sourceclear.com/ci.sh | sh

maven_build:
  stage: build
  script:
    - mvn clean package